apiVersion: apps/v1
kind: Deployment

metadata:
  name: frontend
  namespace: docu-scale

spec:
  replicas: 1

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      containers:
        - name: frontend

          image: docu-frontend

          imagePullPolicy: Never

          ports:
            - containerPort: 5173